"""施設の表記揺れ / 旧名称を canonical 名にマップする alias 定義。

`half_yearly_facility` ETL は HTML 一覧から canonical 名 (現サイトの表記) で
`facilities` テーブルを **DELETE → INSERT** で全件入替する設計のため、 alias を
本番 DB に SQL で直接 UPDATE しても次回 ETL 実行で消える。

このため alias は **コードベースで持つ** 必要があり、 `facility_parser` が
record 生成時に `record.aliases` へこの dict から merge する形にしている。

主な用途:
- `wayback_backfill` で過去 PDF (令和 4 年 `r4-4nyuusyonaiteisisuu.pdf` 等) を
  parse する際、 PDF 内の施設名が現サイトの canonical 名と異なるケースを救済する
- 想定ケース:
  - **改名**: `湘南台もりのこ保育園` → `まなびの森　湘南台もりのこ保育園`
    (運営者の brand 変更で prefix が付いた)
  - **表記揺れ**: `キディ湘南Ｃ-Ｘ` (full-width) ↔ `キディ湘南C-X` (half-width)
  - **合体表記**: `善行保育園・善行乳児保育園` (本来 2 施設) を一方に寄せる
  - **(分園) 表記の括弧違い**: `キディ鵠沼・藤沢（分園）` ↔ `キディ鵠沼・藤沢(分園)`
  - **ひらがな↔カタカナ**: `キッズワン弥勒寺保育園` ↔ `きっずワン弥勒寺保育園`

R4 PDF 113 名のうち、 alias 未投入時点で resolver が NoMatch する 16 件 / 誤
match する 2 件を、 11 個の alias で覆う。 残り 5-7 件は (1) 閉園済 / (2) 真の
新設施設 / (3) Docling header 誤抽出 (`法人立` 等) で alias では救済できない。

新しい mapping を追加する際は、 本 dict の key (canonical 名) が現 facilities
テーブルに必ず存在することを確認すること。 確認方法は
`docs/SETUP.md` の DB 接続手順を参照。
"""

from __future__ import annotations

# canonical 名 (現サイト表記) → 旧名 / 表記揺れ / 合体表記 のリスト
R4_FACILITY_ALIASES: dict[str, list[str]] = {
    # ─── 改名 (運営 brand prefix 等) ───
    "まなびの森　湘南台もりのこ保育園": ["湘南台もりのこ保育園"],
    "まなびの森　藤沢もりのこ保育園": ["藤沢もりのこ保育園"],
    "Gakken保育園藤沢SST": [
        "Gakkenほいくえん藤沢SST（ココファン・ナーサリー藤沢SST)",
    ],
    "湘南台よつば保育園プラス": ["湘南台よつば保育園Ｐｌｕｓ"],
    "藤沢よつば保育園プラス": ["藤沢よつば保育園第二"],

    # ─── 表記揺れ (全角/半角・括弧形式) ───
    "キディ湘南C-X": ["キディ湘南Ｃ-Ｘ"],
    "キディ鵠沼・藤沢(分園)": ["キディ鵠沼・藤沢（分園）"],

    # ─── ひらがな↔カタカナ ───
    "きっずワン弥勒寺保育園": ["キッズワン弥勒寺保育園"],

    # ─── 合体表記 / sub-class 表記 ───
    # `ときわぎ保育園(2歳コース）` は parent 施設の 2歳児クラス専用エントリ。
    # parent と同じ facility_id に寄せる。
    "ときわぎ保育園": ["ときわぎ保育園(2歳コース）"],
    # 分園と本園の合体表記は分園側に寄せる (R4 PDF の min_index は本園ベース)。
    "ときわぎ保育園(分園)": ["ときわぎ保育園・ときわぎ保育園分園"],
    # 善行は本園と乳児保育園が併設で、 R4 PDF は本園名で公開されていた前提で
    # 本園側に寄せる。 乳児園にも将来追加する余地あり。
    "善行保育園": ["善行保育園・善行乳児保育園"],
}


__all__ = ["R4_FACILITY_ALIASES"]
