"""llm-security-lab `shared/` パッケージ。

Phase 0a (本 PR) では空のパッケージマーカーのみ。 Phase 0b で
`shared/llm_runtime/` を、 Phase 0c で `shared/attacks/` / `shared/defenses/` /
`shared/eval/` を実装する (proposal 0008 §3.2)。
"""

__version__ = "0.1.0"
