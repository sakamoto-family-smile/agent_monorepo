# --- App ---
APP_ENV=dev                     # dev | staging | prod
SERVICE_VERSION=0.1.0
LOG_LEVEL=INFO

# --- DB ---
# 優先: DATABASE_URL (SQLAlchemy URL)
#   sqlite+aiosqlite:///./data/piyolog.db                  (dev/test 既定)
#   postgresql+asyncpg://user:pass@host:5432/dbname        (Cloud SQL prod)
DATABASE_URL=

# 後方互換: DATABASE_URL 未設定で PIYOLOG_DB_PATH があれば SQLite 扱い
PIYOLOG_DB_PATH=./data/piyolog.db

# 起動時に create_all でスキーマを当てる (dev/SQLite)。
# 本番 (Postgres) は alembic で管理するため false にする。
DB_AUTO_CREATE=true

# --- LINE ---
LINE_CHANNEL_SECRET=
LINE_CHANNEL_ACCESS_TOKEN=

# --- Family アクセス制御 ---
# 許可する LINE userId を CSV で列挙 (夫婦 2 人分)
FAMILY_USER_IDS=Uxxxxxxxxxxxxxxxxxxxxxxxxxxxx,Uyyyyyyyyyyyyyyyyyyyyyyyyyyyy
# 家族識別子 (Phase 1 はシングル固定)
FAMILY_ID=default

# --- 子の固定 (Phase 1) ---
DEFAULT_CHILD_ID=default

# --- analytics-platform ---
ANALYTICS_ENABLED=true
ANALYTICS_SERVICE_NAME=piyolog-analytics
ANALYTICS_DATA_DIR=./data/analytics
ANALYTICS_COMPRESS=false
ANALYTICS_CONTENT_INLINE_THRESHOLD_BYTES=8192

# --- OpenTelemetry (optional) ---
OTEL_EXPORTER_OTLP_ENDPOINT=
OTEL_EXPORTER_OTLP_HEADERS=
OTEL_SAMPLING_RATIO=1.0

# --- Upload limits ---
UPLOAD_MAX_BYTES=5242880        # 5MB
