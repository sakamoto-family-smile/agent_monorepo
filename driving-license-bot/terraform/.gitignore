# Terraform local files
.terraform/
*.tfstate
*.tfstate.*
*.tfstate.backup
crash.log
crash.*.log

# 自動生成。bootstrap_gcp.sh が project ごとに作る
backend.tf

# 個人の値。example のみ commit する
terraform.tfvars
*.auto.tfvars

# プロバイダ lock は commit する（.terraform.lock.hcl は除外しない）
